# MMCS 承诺：矩阵 Merkle 树承诺方案详解

> 恢复自作者的[知乎原文](https://zhuanlan.zhihu.com/p/2032400665809507295)。

MMCS 承诺介绍

## 问题背景

如何使用 Merkle 树为单个或多个矩阵进行承诺，使得承诺后数据不可篡改，且可以高效验证矩阵中任意一行的数据确实属于被承诺的内容？

```text
Prover                                          Verifier
   ------                                          --------

                                +-----------+
              (1) Root = Commit(| m00   m01 |)
                                | m10   m11 |
                                +-----------+
   ------------------------------------------------->

                     (2) Query: row 0
   <-------------------------------------------------

              (3) Send: (m00, m01), proof
   ------------------------------------------------->

                                   (4) rebuild Root'
                                       check Root' == Root
```

## 单矩阵 Merkle 树构建

如何将单个矩阵构建成一棵 Merkle 树，从而得到一个固定长度的承诺值？以 4 行 × 3 列矩阵为例：

```text
Matrix Rows           Leaf Digests                 Merkle Tree

[a0, a1, a2]  -->  H(a0,a1,a2) = d0  --+
                                       +--  H(d0,d1) = n0  --+
[b0, b1, b2]  -->  H(b0,b1,b2) = d1  --+                     |
                                                             +--  H(n0,n1) = Root
[c0, c1, c2]  -->  H(c0,c1,c2) = d2  --+                     |
                                       +--  H(d2,d3) = n1  --+
[d0, d1, d2]  -->  H(d0,d1,d2) = d3  --+

H = hash function
```

从图中可以看出构建过程分为三个阶段：

1. **叶子层生成**：矩阵的每一行通过哈希函数 H 映射为摘要（d₀, d₁, d₂, d₃），这些摘要构成 Merkle 树的叶子节点。
2. **逐层压缩**：相邻的两个摘要通过哈希函数 H 合并。例如 d₀ 和 d₁ 合并为 n₀，d₂ 和 d₃ 合并为 n₁。
3. **根节点生成**：继续向上压缩，直到只剩一个根节点，即为承诺值 Root。

对于 4 行矩阵，从叶子到根需要 2 次压缩，树的高度为 2 层。一般地，对于 N 行矩阵，树的高度为 log₂(N) 层。

### 打开与验证

**打开（Open）**：给定行索引，返回该行的原始值和从叶子到根的 Merkle 路径（兄弟节点序列）。

```text
Open row 1:
              Root                  known (commitment)
          ┌────┴────┐
         n0        [n1]             need: n1 (sibling)
      ┌──┴──┐    ┌──┴──┐
    [d0]    d1   d2    d3           need: d0 (sibling)
             |
        [b0, b1, b2]

  Merkle path  = [d0, n1]
  Row raw data = [b0, b1, b2]
```

以打开第 1 行为例：

- **叶子层**：当前节点是 `d₁`（通过对行原始值计算哈希得到），需要兄弟节点 `d₀` 来向上重建。
- **第 1 层**：当前节点是 `n₀`（由 `d₀` 和 `d₁` 压缩得到），需要兄弟节点 `n₁` 来向上重建根节点，验证根节点是否与承诺值相同。

Merkle 路径 `[d₀, n₁]` 包含了从叶子到根路径上所有兄弟节点，验证者可以用它重建出 Root。

## 多矩阵承诺：混合高度注入

如何将多个不同行数的矩阵承诺到同一棵 Merkle 树中，使得它们共享同一个承诺值？以矩阵 A（4 行）和矩阵 B（2 行）为例：

```text
Matrix A (4 rows x 3 cols):          Matrix B (2 rows x 2 cols):
  +---------------------------+         +---------------------+
  | row0: a00  a01  a02       |         | row0: b00  b01      |
  | row1: a10  a11  a12       |         | row1: b10  b11      |
  | row2: a20  a21  a22       |         +---------------------+
  | row3: a30  a31  a32       |
  +---------------------------+

  Merkle tree construction:

  [a00,a01,a02] --> d0 --+
                          +-- H(d0,d1)=t0 ---+
  [a10,a11,a12] --> d1 --+                   +-- H(t0,i0)=n0 --+
  [b00,b01]     --> i0 ----------------------+                 |
  [a20,a21,a22] --> d2 --+                                     |-- H(n0,n1)=Root
                          +-- H(d2,d3)=t1 ---+                 |
  [a30,a31,a32] --> d3 --+                   +-- H(t1,i1)=n1 --+
  [b10,b11]     --> i1 ----------------------+

  H = hash function
```

从图中可以看出构建过程分为多个阶段：

- **叶子层生成**：矩阵 A 的每一行通过哈希函数映射为叶子摘要 `d₀, d₁, d₂, d₃`。
- **第 1 层哈希**：相邻叶子节点哈希得到中间节点 `t₀ = H(d₀, d₁)` 和 `t₁ = H(d₂, d₃)`。
- **第 2 层注入**：矩阵 B 的行哈希 `i₀, i₁` 与中间节点哈希得到 `n₀ = H(t₀, i₀)` 和 `n₁ = H(t₁, i₁)`。
- **根节点生成**：`n₀` 和 `n₁` 哈希得到最终的承诺值 Root。

注入的关键特点是：矩阵 B 不参与叶子层构建，而是在哈希到节点数等于其行数时注入。

### 打开与验证

**打开（Open）**：给定行索引，返回所有矩阵在该索引对应的行原始值和 Merkle 路径。以打开第 `i = 2` 行为例：

```text
Open row i = 2  (Matrix A row index = 2,  Matrix B row index = 2 >> 1 = 1):

                            Root                     known (commitment)
                    ┌────────┴───────┐
                  [n0]               n1             need: n0 (sibling)
              ┌────┴────┐       ┌────┴────┐
             t0        i0      t1        i1          need: i1 (inject sibling)
          ┌──┴──┐           ┌──┴──┐       |
         d0     d1         d2   [d3]  [b10, b11]
                            |
                     [a20, a21, a22]

  Merkle path        = [d3, n0]
  Matrix A row data  = [a20, a21, a22]
  Matrix B row data  = [b10, b11]
```

**验证（Verify）**：用 Merkle 路径从叶子重建到根，验证过程与承诺时的注入过程对称：

- **Layer 0（数据层）**：当前节点是 `d₂`（通过对矩阵 A 的第二行哈希得到），需要兄弟节点 `d₃` 来向上重建。
- **Layer 1**：当前节点是 `t₁`（由 `d₂` 和 `d₃` 压缩得到），需要注入点兄弟 `i₁`（通过对矩阵 B 的第一行哈希得到）来向上重建。
- **Layer 2**：当前节点是 `n₁`（由 `t₁` 和 `i₁` 压缩得到），需要兄弟节点 `n₀` 来向上重建根节点。
- **Layer 3（根）**：由 `n₀` 和 `n₁` 压缩得到 Root，验证根节点是否与承诺值相同。

Merkle 路径 `[d₃, n₀]` 包含了从叶子到根路径上所有兄弟节点（包括注入点兄弟），验证者可以用它重建出 Root。
